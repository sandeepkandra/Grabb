<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kitchengrabbngo">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_ASSISTED_GPS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-sdk tools:overrideLibrary="org.lucasr.twowayview, org.lucasr.twowayview.widget, jp.wasabeef.recyclerview" />
    <application
        android:allowBackup="true"
        android:name=".utility.MyApp"
        android:icon="@drawable/logoround"
        android:label="@string/app_name"
        tools:replace="android:icon,android:theme"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->

       <!--
       relase apk lenovo sandy
       AIzaSyDiyINj1FtxzhCdQvvoUuO3ZneI6LLZPKM
       debug lenovo

       AIzaSyC9waW8nS0eXJhrbmU4a4J1j7yDgu2W0G4

       -->

        <activity
            android:name=".ativities.Login"
            android:label="Grabb-n-Go"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ativities.SettingActivity"
            android:label="Setting"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ativities.Inventory"
            android:label="Inventory"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ativities.MapsActivity"
            android:label="Map"
            android:screenOrientation="portrait"></activity>

        <meta-data

            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC9waW8nS0eXJhrbmU4a4J1j7yDgu2W0G4" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="2e098e4df53037df50018a79f46fb5b5e0b1f21d" />
    </application>

</manifest>